// src/components/Dashboard.js
import React from 'react';

const Dashboard = ({ token }) => {
  // You can use the token here to fetch user-specific data if needed
  return (
    <div>
      <h2>Dashboard</h2>
      <p>This is the user dashboard.</p>
    </div>
  );
};

export default Dashboard;
